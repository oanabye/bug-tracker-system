<Window x:Class="BugTracker.Client.Views.ShareBugWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Share Bug" Height="460" Width="460"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#0f172a">

    <Window.Resources>
        <Style x:Key="ActionBtn" TargetType="Button">
            <Setter Property="Height" Value="36"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="16" Padding="18,0">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="FieldLabel" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#475569"/>
            <Setter Property="FontSize" Value="10"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Margin" Value="2,0,0,5"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="54"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- HEADER -->
        <Border Grid.Row="0" Background="#16213e"
                BorderBrush="#1e293b" BorderThickness="0,0,0,1">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="18,0">
                <TextBlock Text="🔗" FontSize="18" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <TextBlock Text="Share Bug" Foreground="White"
                           FontSize="16" FontWeight="Medium" VerticalAlignment="Center"/>
                <TextBlock Text=" — with Developer" Foreground="#64748b"
                           FontSize="16" VerticalAlignment="Center"/>
            </StackPanel>
        </Border>

        <!-- FORM -->
        <Grid Grid.Row="1" Margin="24,18,24,18">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="14"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="8"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="14"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="8"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Bug title badge -->
            <Border Grid.Row="0" Background="#1e293b" CornerRadius="12"
                    Padding="14,10" BorderBrush="#1e3a5f" BorderThickness="1">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="🐛 " FontSize="13" VerticalAlignment="Center"/>
                    <TextBlock x:Name="TxtBugTitle"
                               Foreground="#e2e8f0" FontSize="13" FontWeight="Medium"
                               TextTrimming="CharacterEllipsis" VerticalAlignment="Center"/>
                </StackPanel>
            </Border>

            <!-- Developer label -->
            <TextBlock Grid.Row="2" Text="TRIMITE CĂTRE DEVELOPER"
                       Style="{StaticResource FieldLabel}"/>

            <!-- Developer ComboBox -->
            <Border Grid.Row="4" Background="#1e293b" CornerRadius="10"
                    BorderBrush="#334155" BorderThickness="1">
                <ComboBox x:Name="CmbDevelopers"
                          Height="38" Background="Transparent"
                          Foreground="#e2e8f0" BorderThickness="0"
                          Padding="10,0" FontSize="13"
                          DisplayMemberPath="Username">
                    <ComboBox.Resources>
                        <Style TargetType="ComboBoxItem">
                            <Setter Property="Background" Value="#1e293b"/>
                            <Setter Property="Foreground" Value="#e2e8f0"/>
                            <Setter Property="Padding" Value="10,6"/>
                        </Style>
                    </ComboBox.Resources>
                </ComboBox>
            </Border>

            <!-- Message label -->
            <TextBlock Grid.Row="6" Text="MESAJ / INSTRUCȚIUNI"
                       Style="{StaticResource FieldLabel}"/>

            <!-- Message TextBox -->
            <Border Grid.Row="8" Background="#1e293b" CornerRadius="12"
                    BorderBrush="#334155" BorderThickness="1">
                <TextBox x:Name="TxtMessage" Height="80"
                         Background="Transparent" Foreground="#94a3b8"
                         BorderThickness="0" Padding="10,8"
                         FontSize="13" CaretBrush="White"
                         TextWrapping="Wrap" AcceptsReturn="True"
                         VerticalScrollBarVisibility="Auto"/>
            </Border>

            <!-- Buttons -->
            <StackPanel Grid.Row="10" Orientation="Horizontal"
                        HorizontalAlignment="Right">
                <Button Content="Cancel" Width="95"
                        Style="{StaticResource ActionBtn}"
                        Background="#374151" Margin="0,0,10,0"
                        Click="BtnCancel_Click"/>
                <Button Content="📤  Share" Width="110"
                        Style="{StaticResource ActionBtn}"
                        Background="#7c3aed"
                        Click="BtnShare_Click"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
