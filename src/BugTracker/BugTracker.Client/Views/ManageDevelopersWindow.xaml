<Window x:Class="BugTracker.Client.Views.ManageDevelopersWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Manage Developers" Height="560" Width="600"
        WindowStartupLocation="CenterOwner"
        Background="#0f172a">

    <Window.Resources>
        <Style x:Key="ActionBtn" TargetType="Button">
            <Setter Property="Height" Value="34"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="16" Padding="14,0">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="FieldLabel" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#475569"/>
            <Setter Property="FontSize" Value="10"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Margin" Value="2,0,0,5"/>
        </Style>
        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#16213e"/>
            <Setter Property="Foreground" Value="#94a3b8"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
            <Setter Property="BorderBrush" Value="#1e3a5f"/>
        </Style>
        <Style TargetType="DataGridCell">
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Foreground" Value="#e2e8f0"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DataGridCell">
                        <Border Padding="12,0" Background="{TemplateBinding Background}">
                            <ContentPresenter VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="DataGridRow">
            <Setter Property="Height" Value="40"/>
            <Setter Property="Background" Value="#0f172a"/>
            <Style.Triggers>
                <Trigger Property="AlternationIndex" Value="1">
                    <Setter Property="Background" Value="#111827"/>
                </Trigger>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#1e3a5f"/>
                </Trigger>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#1e3a5f"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style TargetType="ScrollBar">
            <Setter Property="Width" Value="6"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ScrollBar">
                        <Grid>
                            <Track x:Name="PART_Track" IsDirectionReversed="True">
                                <Track.Thumb>
                                    <Thumb>
                                        <Thumb.Template>
                                            <ControlTemplate TargetType="Thumb">
                                                <Border Background="#334155" CornerRadius="3" Margin="1"/>
                                            </ControlTemplate>
                                        </Thumb.Template>
                                    </Thumb>
                                </Track.Thumb>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="54"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- HEADER -->
        <Border Grid.Row="0" Background="#16213e"
                BorderBrush="#1e293b" BorderThickness="0,0,0,1">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="18,0">
                <TextBlock Text="👥" FontSize="18" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <TextBlock Text="Manage" Foreground="White"
                           FontSize="16" FontWeight="Medium" VerticalAlignment="Center"/>
                <TextBlock Text=" — Developers" Foreground="#64748b"
                           FontSize="16" VerticalAlignment="Center"/>
            </StackPanel>
        </Border>

        <!-- CONTENT -->
        <Grid Grid.Row="1" Margin="20,16,20,16">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Add Developer card -->
            <Border Grid.Row="0" Background="#1e293b" CornerRadius="14"
                    Padding="16,14" Margin="0,0,0,14"
                    BorderBrush="#1e3a5f" BorderThickness="1">
                <StackPanel>
                    <TextBlock Text="ADD NEW DEVELOPER" Style="{StaticResource FieldLabel}"
                               Margin="0,0,0,10"/>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <Border Grid.Column="0" Background="#0f172a" CornerRadius="10"
                                BorderBrush="#334155" BorderThickness="1" Margin="0,0,8,0">
                            <TextBox x:Name="TxtNewUsername"
                                     Height="34" Background="Transparent"
                                     Foreground="#e2e8f0" BorderThickness="0"
                                     Padding="10,0" FontSize="13"
                                     CaretBrush="White"
                                     VerticalContentAlignment="Center"/>
                        </Border>

                        <Border Grid.Column="1" Background="#0f172a" CornerRadius="10"
                                BorderBrush="#334155" BorderThickness="1" Margin="0,0,8,0">
                            <PasswordBox x:Name="TxtNewPassword"
                                         Height="34" Background="Transparent"
                                         Foreground="#e2e8f0" BorderThickness="0"
                                         Padding="10,0" FontSize="13"/>
                        </Border>

                        <Button Grid.Column="2" Content="➕  Add"
                                Width="88" Style="{StaticResource ActionBtn}"
                                Background="#059669"
                                Click="BtnAdd_Click"/>
                    </Grid>
                    <TextBlock x:Name="TxtAddError" Foreground="#f87171"
                               FontSize="12" Margin="2,8,0,0"/>
                </StackPanel>
            </Border>

            <!-- Developers DataGrid -->
            <Border Grid.Row="1" Background="#0f172a" CornerRadius="14"
                    BorderBrush="#1e3a5f" BorderThickness="1"
                    ClipToBounds="True">
                <DataGrid x:Name="DevsGrid"
                          Background="Transparent"
                          Foreground="#e2e8f0"
                          BorderThickness="0"
                          AutoGenerateColumns="False"
                          IsReadOnly="True"
                          GridLinesVisibility="Horizontal"
                          HorizontalGridLinesBrush="#1e293b"
                          AlternationCount="2"
                          RowHeaderWidth="0">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="ID"
                                            Binding="{Binding Id}" Width="60"/>
                        <DataGridTextColumn Header="Username"
                                            Binding="{Binding Username}" Width="*"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Border>

            <!-- Action Buttons -->
            <StackPanel Grid.Row="2" Orientation="Horizontal"
                        HorizontalAlignment="Right" Margin="0,14,0,0">
                <Button Content="🔑  Update Password" Width="152"
                        Style="{StaticResource ActionBtn}"
                        Background="#d97706" Margin="0,0,10,0"
                        Click="BtnUpdatePassword_Click"/>
                <Button Content="🗑️  Delete Developer" Width="152"
                        Style="{StaticResource ActionBtn}"
                        Background="#dc2626"
                        Click="BtnDelete_Click"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
